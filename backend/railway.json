{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && echo '=== STARTING APP ===' && node dist/main 2>&1 || (echo '=== APP CRASHED ===' && sleep 60)",
    "restartPolicyType": "ON_FAILURE"
  }
}
